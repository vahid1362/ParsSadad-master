@model QtasHelpDesk.ViewModels.Content.PostViewModel
@{
    ViewData["Title"] = "ShowPost";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header" style="">
        <h5 class="panel-title entry-title" itemprop="headline_name">
            @Model.Title
        </h5>
    </div>
    <div class="card-body">  @Model.Summary</div>
    <div class="card-footer"> <a href="@Url.Action("ShowPdf","Post", new{postId=@Model.Id})">نمایش فایل</a></div>

</div>
